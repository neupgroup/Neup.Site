
{
  "entities": {
    "Site": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Site",
      "type": "object",
      "description": "Represents a website created within Neup.Sites.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Site entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the website."
        },
        "logoUrl": {
          "type": "string",
          "description": "URL of the website's logo image.",
          "format": "uri"
        },
        "hideSitename": {
          "type": "boolean",
          "description": "Whether to hide the site name in the header, useful if the logo contains the name."
        },
        "description": {
            "type": "string",
            "description": "A short description of the site."
        },
        "socialProfiles": {
            "type": "array",
            "description": "A list of social media profiles.",
            "items": {
                "type": "object",
                "properties": {
                    "platformName": { "type": "string" },
                    "url": { "type": "string", "format": "uri" }
                },
                "required": ["platformName", "url"]
            }
        },
        "contactEmail": {
            "type": "array",
            "description": "A list of contact emails.",
            "items": {
                "type": "object",
                "properties": {
                    "value": { "type": "string", "format": "email" }
                }
            }
        },
        "contactPhone": {
            "type": "array",
            "description": "A list of contact phone numbers.",
            "items": {
                "type": "object",
                "properties": {
                    "value": { "type": "string" }
                }
            }
        },
        "status": {
          "type": "string",
          "description": "The current status of the site (e.g., pending, active, inactive).",
          "format": "string"
        },
        "type": {
          "type": "string",
          "description": "The type of website (e.g., real estate, news portal, academic institution, personal portfolio, corporate portfolio)."
        },
        "modules": {
          "type": "object",
          "description": "A map of feature modules and their activation status for the site.",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "active": {
                "type": "boolean",
                "description": "Whether the module is active for the site."
              },
              "enabledOn": {
                "type": "string",
                "format": "date-time",
                "description": "The date and time when the module was enabled."
              },
              "expiresOn": {
                "type": "string",
                "format": "date-time",
                "description": "The optional date and time when the module access expires."
              }
            },
            "required": [
              "active"
            ]
          }
        }
      },
      "required": [
        "id",
        "name",
        "status",
        "type"
      ]
    },
    "Page": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Page",
      "type": "object",
      "description": "Represents a page within a website.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Page entity."
        },
        "siteId": {
          "type": "string",
          "description": "Reference to Site. (Relationship: Site 1:N Page)"
        },
        "path": {
          "type": "string",
          "description": "The URL path for the page (e.g., /onboarding)."
        },
        "content": {
          "type": "string",
          "description": "The HTML content of the page."
        },
        "requiresPendingSite": {
          "type": "boolean",
          "description": "Whether this page requires the site to be in a pending state to be accessible."
        }
      },
      "required": [
        "id",
        "siteId",
        "path",
        "content",
        "requiresPendingSite"
      ]
    },
    "Path": {
      "$schema": "http://json-schema.org/draft-07_schema#",
      "title": "Path",
      "type": "object",
      "description": "Represents a URL path that maps to a page.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the path."
        },
        "siteId": {
          "type": "string",
          "description": "The ID of the site this path belongs to."
        },
        "pageId": {
          "type": "string",
          "description": "The ID of the page this path maps to."
        },
        "path": {
          "type": "string",
          "description": "The URL path (e.g., /about, /products/cool-widget)."
        },
        "createdAt": {
            "type": "string",
            "format": "date-time"
        }
      },
      "required": ["id", "siteId", "pageId", "path"]
    },
    "NewsArticle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "News Article",
      "type": "object",
      "description": "Represents a news article.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier, formatted as [slug]-[random-string]." },
        "title": { "type": "string", "description": "The title of the article." },
        "content": { "type": "string", "description": "The main content of the article, likely in Markdown." },
        "author": { "type": "string", "description": "The name of the author." },
        "imageUrl": { "type": "string", "format": "uri", "description": "URL for a feature image." },
        "publishedAt": { "type": "string", "format": "date-time", "description": "The date and time the article was published." },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "content", "author", "publishedAt"]
    },
     "Server": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Server",
      "type": "object",
      "description": "Represents a physical or virtual server for deployments.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the server." },
        "name": { "type": "string", "description": "A friendly name for the server." },
        "publicIp": { "type": "string", "format": "ipv4", "description": "The public IP address of the server." },
        "privateIp": { "type": "string", "format": "ipv4", "description": "The private IP address of the server." },
        "privateKey": { "type": "string", "description": "The SSH private key for accessing the server." },
        "serverType": { "type": "string", "enum": ["vps", "dedicated", "cloud"], "description": "The type of the server." },
        "provider": { "type": "string", "description": "The hosting provider (e.g., AWS, DigitalOcean)." },
        "portsOpen": { "type": "array", "items": { "type": "number" }, "description": "A list of open ports." },
        "usedPorts": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "port": { "type": "number" },
                    "description": { "type": "string" }
                },
                "required": ["port", "description"]
            },
            "description": "A list of ports that are currently in use."
        },
        "storageUsed": { "type": "string", "description": "Used storage space (e.g., '5.2')." },
        "storageTotal": { "type": "string", "description": "Total storage space (e.g., '25')." },
        "storageUnit": { "type": "string", "description": "The unit for storage space (e.g., 'G', 'T')." },
        "isPrivate": { "type": "boolean", "description": "Indicates if the server is on a private network." },
        "username": { "type": "string", "description": "The default SSH username for the server." },
        "basePath": { "type": "string", "description": "The default base path for deployments on the server." },
        "createdOn": { "type": "string", "format": "date-time" },
        "expiresOn": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "publicIp", "privateKey"]
    },
     "ServerLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Server Log",
      "type": "object",
      "description": "Represents a log entry for a command executed on a server.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the log." },
        "serverId": { "type": "string", "description": "The ID of the server where the command was run." },
        "command": { "type": "string", "description": "The command that was executed." },
        "output": { "type": "string", "description": "The output or result of the command." },
        "status": { "type": "string", "enum": ["pending", "ongoing", "completed", "failed", "cancelled"], "description": "The status of the command execution." },
        "initiatedBy": { "type": "string", "description": "Identifier for the user or system that initiated the command." },
        "initiatedAt": { "type": "string", "format": "date-time", "description": "When the command was logged." },
        "completedAt": { "type": "string", "format": "date-time", "description": "When the command finished execution." }
      },
      "required": ["serverId", "command", "output", "status", "initiatedBy", "initiatedAt"]
    },
    "ServerCommand": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Server Command",
      "type": "object",
      "description": "A template for a command that can be run on a server.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "commandTemplate": { "type": "string", "description": "The command string with placeholders like {{parameterKey}}." },
        "parameters": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "key": { "type": "string" },
              "label": { "type": "string" },
              "type": { "type": "string", "enum": ["string", "number", "boolean"] },
              "defaultValue": { "type": "string" }
            },
            "required": ["key", "label", "type"]
          }
        },
        "preExecutionScript": { "type": "string", "description": "JavaScript code to run before command execution." },
        "type": {
          "type": "string",
          "enum": ["creation", "destruction", "updation", "view"],
          "description": "The type or category of the command."
        },
        "danger": {
            "type": "string",
            "enum": ["low", "mid", "high"],
            "description": "The danger level associated with running the command."
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "commandTemplate"]
    },
    "CodeFile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Code File",
      "type": "object",
      "description": "Represents a single file in the user's codebase.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the file." },
        "siteId": { "type": "string", "description": "The ID of the site this file belongs to." },
        "fileName": { "type": "string", "description": "The name of the file (e.g., 'index.js')." },
        "filePath": { "type": "string", "description": "The full path of the file within the project structure (e.g., 'src/components/Button.tsx')." },
        "content": { "type": "string", "description": "The base64 encoded content of the file." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["siteId", "fileName", "filePath", "content"]
    },
    "LinkedAccount": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Linked Account",
      "type": "object",
      "description": "Represents a third-party account linked to a user.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the linked account." },
        "account_id": { "type": "string", "description": "The ID of the user this account is linked to." },
        "platform": { "type": "string", "enum": ["github", "google"], "description": "The OAuth provider." },
        "authorized_on": { "type": "string", "format": "date-time", "description": "Timestamp of when the authorization was granted." },
        "authorization_info": { 
            "type": "object", 
            "description": "A JSON object containing all relevant tokens and user information from the provider.",
            "properties": {
                "access_token": { "type": "string" },
                "refresh_token": { "type": "string" },
                "scope": { "type": "string" },
                "provider_user_id": { "type": "string" },
                "provider_username": { "type": "string" }
            },
            "required": ["access_token", "scope", "provider_user_id"]
        }
      },
      "required": ["id", "account_id", "platform", "authorized_on", "authorization_info"]
    },
    "Team": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Team",
      "type": "object",
      "description": "Represents a team or group of members.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "order": { "type": "number", "description": "Order for display purposes." }
      },
      "required": ["id", "name"]
    },
    "Member": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Member",
      "type": "object",
      "description": "Represents a team member.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string" },
        "imageUrl": { "type": "string", "format": "uri" },
        "teamIds": { "type": "array", "items": { "type": "string" } },
        "permissions": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["id", "name", "email"]
    },
     "JobPosting": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Job Posting",
      "type": "object",
      "description": "Represents a job posting for the hiring module.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "location": { "type": "string" },
        "type": { "type": "string", "enum": ["Full-time", "Part-time", "Contract", "Internship"] },
        "status": { "type": "string", "enum": ["Draft", "Open", "Closed"] },
        "qualifications": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "status"]
    },
    "Applicant": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Applicant",
      "type": "object",
      "description": "Represents an applicant for a job posting.",
      "properties": {
        "id": { "type": "string" },
        "jobId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "resumeUrl": { "type": "string", "format": "uri" },
        "coverLetter": { "type": "string" },
        "status": { "type": "string", "enum": ["New", "Reviewing", "Interview", "Offered", "Hired", "Rejected"] },
        "appliedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "jobId", "name", "email", "status", "appliedAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/sites/{siteId}",
        "definition": {
          "entityName": "Site",
          "schema": {
            "$ref": "#/backend/entities/Site"
          },
          "description": "Stores site information. The 'siteId' is the unique identifier for the site.",
          "params": [
            {
              "name": "siteId",
              "description": "The unique identifier for the site."
            }
          ]
        }
      },
      {
        "path": "/sites/{siteId}/pages/{pageId}",
        "definition": {
          "entityName": "Page",
          "schema": {
            "$ref": "#/backend/entities/Page"
          },
          "description": "Stores page information associated with a specific site. The 'siteId' is the site's unique identifier, and 'pageId' is the page's unique identifier.",
          "params": [
            {
              "name": "siteId",
              "description": "The unique identifier for the site."
            },
            {
              "name": "pageId",
              "description": "The unique identifier for the page."
            }
          ]
        }
      },
      {
        "path": "/news/{newsId}",
        "definition": {
          "entityName": "NewsArticle",
          "schema": {
            "$ref": "#/backend/entities/NewsArticle"
          },
          "description": "Stores individual news articles. The document ID is a combination of a slug and a random ID.",
          "params": [
            {
              "name": "newsId",
              "description": "The unique identifier for the news article."
            }
          ]
        }
      },
       {
        "path": "/paths/{pathId}",
        "definition": {
          "entityName": "Path",
          "schema": {
            "$ref": "#/backend/entities/Path"
          },
          "description": "Stores URL path mappings to pages.",
          "params": [
            {
              "name": "pathId",
              "description": "The unique identifier for the path mapping."
            }
          ]
        }
      },
      {
        "path": "/servers/{serverId}",
        "definition": {
          "entityName": "Server",
          "schema": {
            "$ref": "#/backend/entities/Server"
          },
          "description": "Stores server information.",
          "params": [
            {
              "name": "serverId",
              "description": "The unique identifier for the server."
            }
          ]
        }
      },
      {
        "path": "/serverLogs/{logId}",
        "definition": {
          "entityName": "ServerLog",
          "schema": {
            "$ref": "#/backend/entities/ServerLog"
          },
          "description": "Stores logs of commands executed on servers.",
          "params": [
            {
              "name": "logId",
              "description": "The unique identifier for the server log entry."
            }
          ]
        }
      },
      {
        "path": "/serverCommands/{commandId}",
        "definition": {
          "entityName": "ServerCommand",
          "schema": {
            "$ref": "#/backend/entities/ServerCommand"
          },
          "description": "Stores reusable server command templates.",
          "params": [
            {
              "name": "commandId",
              "description": "The unique identifier for the command."
            }
          ]
        }
      },
      {
        "path": "/codeFiles/{codeFileId}",
        "definition": {
          "entityName": "CodeFile",
          "schema": {
            "$ref": "#/backend/entities/CodeFile"
          },
          "description": "Stores user-uploaded code files for custom hosting.",
          "params": [
            {
              "name": "codeFileId",
              "description": "The unique identifier for the code file."
            }
          ]
        }
      },
      {
        "path": "/linked_accounts/{linkedAccountId}",
        "definition": {
          "entityName": "LinkedAccount",
          "schema": {
            "$ref": "#/backend/entities/LinkedAccount"
          },
          "description": "Stores third-party account connections for a user.",
          "params": [
            {
              "name": "linkedAccountId",
              "description": "The unique identifier for the linked account."
            }
          ]
        }
      },
      {
        "path": "/teams/{teamId}",
        "definition": {
          "entityName": "Team",
          "schema": { "$ref": "#/backend/entities/Team" },
          "description": "Stores team information."
        }
      },
      {
        "path": "/members/{memberId}",
        "definition": {
          "entityName": "Member",
          "schema": { "$ref": "#/backend/entities/Member" },
          "description": "Stores information about individual team members."
        }
      },
      {
        "path": "/hiring/{jobId}",
        "definition": {
          "entityName": "JobPosting",
          "schema": { "$ref": "#/backend/entities/JobPosting" },
          "description": "Stores job postings for the hiring module."
        }
      },
       {
        "path": "/hiring/{jobId}/applicants/{applicantId}",
        "definition": {
          "entityName": "Applicant",
          "schema": { "$ref": "#/backend/entities/Applicant" },
          "description": "Stores applicants for a specific job posting."
        }
      }
    ],
    "reasoning": "This design prioritizes security and scalability by adhering to the principles of Authorization Independence, Structural Segregation, Access Modeling, and Data Clarity. \n\nThe `sites` collection stores site data. The `pages` subcollection stores page data related to the site.\n\n**Authorization Independence**: No `get()` calls are needed in the security rules because access control is based on the `status` field in the `sites` collection and `requiresPendingSite` field in the `pages` collection. All required attributes for pages authorization are denormalized into the page document itself, removing the need for hierarchical dependencies.\n\n**Structural Segregation**: The `sites` collection is dedicated solely to site data, ensuring a homogeneous security posture. Similarly, the `pages` subcollection only contains page documents associated to a specific site.\n\n**Access Modeling**: The structure uses path-based ownership (`/sites/{siteId}/pages/{pageId}`).\n\n**QAPs**: The segregation of sites and pages based on the site ID supports efficient and secure list operations, as queries can be scoped to a specific site.\n\n**Invariants**: The structure supports invariants by associating pages to sites via the `siteId` field in the `pages` subcollection. This ensures the integrity of the 1:N relationship between sites and pages."
  }
}
